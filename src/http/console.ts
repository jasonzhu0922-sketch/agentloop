export const CONSOLE_HTML = `<!doctype html>
<html lang="zh-CN">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="theme-color" content="#0b1013">
  <title>DeepSeek Harness · AgentLoop</title>
  <link rel="stylesheet" href="/assets/app.css">
</head>
<body>
  <div id="auth-root"></div>
  <div id="app-root" hidden></div>
  <div id="notice" role="status" aria-live="polite"></div>
  <script type="module" src="/assets/app.js"></script>
</body>
</html>`;

export const CONSOLE_CSS = `:root{color-scheme:dark;--ink:#e9f3ef;--muted:#94a7a2;--faint:#667a74;--ground:#0b1013;--sidebar:#101719;--panel:#151f21;--panel-2:#192527;--line:#29383a;--accent:#9de46d;--accent-ink:#18300d;--cyan:#72d9d0;--amber:#e8bd71;--rose:#ed8179;--shadow:0 20px 70px #0008;font-family:Inter,ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}*{box-sizing:border-box}body{margin:0;min-width:320px;background:radial-gradient(circle at 82% -20%,#203531 0,transparent 33%),radial-gradient(circle at 35% 100%,#182d2c 0,transparent 38%),var(--ground);color:var(--ink);font-size:14px;line-height:1.48}button,input,textarea,select{font:inherit}button{cursor:pointer}.auth-shell{min-height:100vh;display:grid;place-items:center;padding:28px}.auth-card{width:min(100%,470px);padding:32px;border:1px solid var(--line);border-radius:24px;background:linear-gradient(145deg,#172123e8,#101719ed);box-shadow:var(--shadow)}.eyebrow{margin:0 0 11px;color:var(--accent);font:600 11px/1.2 ui-monospace,SFMono-Regular,Menlo,monospace;letter-spacing:.14em;text-transform:uppercase}.auth-card h1{margin:0;font-size:31px;letter-spacing:-.045em}.auth-card p{margin:12px 0 26px;color:var(--muted)}.stack{display:grid;gap:13px}.field{display:grid;gap:7px;color:var(--muted);font-size:12px}.field input,.field textarea,.field select,.composer textarea{width:100%;border:1px solid var(--line);border-radius:10px;background:#0c1315;color:var(--ink);padding:10px 12px;outline:none;transition:border .15s,box-shadow .15s}.field input:focus,.field textarea:focus,.field select:focus,.composer textarea:focus{border-color:var(--cyan);box-shadow:0 0 0 3px #72d9d024}.field textarea{min-height:100px;resize:vertical}.button-row{display:flex;flex-wrap:wrap;gap:9px}.button{border:1px solid transparent;border-radius:10px;background:var(--accent);color:var(--accent-ink);padding:10px 14px;font-weight:750;cursor:pointer;transition:transform .15s,filter .15s}.button:hover{filter:brightness(1.06);transform:translateY(-1px)}.button:disabled{cursor:wait;opacity:.62;transform:none}.button.secondary{border-color:var(--line);background:#1a2728;color:var(--ink)}.button.ghost{border-color:var(--line);background:transparent;color:var(--muted)}.app-shell{min-height:100vh;display:grid;grid-template-columns:264px minmax(0,1fr)}.sidebar{position:sticky;top:0;height:100vh;display:flex;flex-direction:column;padding:20px 14px;border-right:1px solid var(--line);background:linear-gradient(180deg,#11191b,#0d1315)}.brand{display:flex;align-items:center;gap:11px;padding:5px 10px 23px}.brand-mark{display:grid;place-items:center;width:32px;height:32px;border:1px solid #b4f38c66;border-radius:9px;background:linear-gradient(135deg,#a9ed77,#63cfc3);color:#12220d;font-weight:900}.brand strong{display:block;font-size:15px;letter-spacing:-.02em}.brand small{display:block;color:var(--muted);font:11px ui-monospace,SFMono-Regular,Menlo,monospace}.nav-label{margin:18px 10px 7px;color:var(--faint);font:10px/1.2 ui-monospace,SFMono-Regular,Menlo,monospace;letter-spacing:.12em;text-transform:uppercase}.nav-button{display:flex;align-items:center;gap:10px;width:100%;border:0;border-radius:9px;padding:10px;background:transparent;color:var(--muted);text-align:left;cursor:pointer}.nav-button:hover,.nav-button.active{background:#1b2829;color:var(--ink)}.nav-button.active{box-shadow:inset 2px 0 var(--accent)}.nav-icon{width:18px;color:var(--cyan);font:600 12px ui-monospace,SFMono-Regular,Menlo,monospace}.sidebar-bottom{margin-top:auto;padding:15px 10px 2px;border-top:1px solid var(--line)}.identity{overflow:hidden;color:var(--muted);font-size:12px;text-overflow:ellipsis;white-space:nowrap}.identity b{display:block;overflow:hidden;color:var(--ink);font-weight:650;text-overflow:ellipsis}.main{min-width:0;padding:22px 28px 40px}.topbar{display:flex;align-items:center;justify-content:space-between;gap:18px;min-height:46px;margin-bottom:21px}.crumb{color:var(--muted);font-size:12px}.crumb b{color:var(--ink);font-weight:650}.status-pill{display:inline-flex;align-items:center;gap:8px;border:1px solid var(--line);border-radius:999px;padding:7px 11px;background:#111a1b;color:var(--muted);font-size:12px}.status-dot{width:7px;height:7px;border-radius:99px;background:var(--accent);box-shadow:0 0 0 3px #9de46d1f}.view-head{display:flex;align-items:end;justify-content:space-between;gap:15px;margin-bottom:18px}.view-head h1{margin:0;font-size:26px;letter-spacing:-.045em}.view-head p{max-width:650px;margin:7px 0 0;color:var(--muted)}.work-grid{display:grid;grid-template-columns:minmax(0,1.4fr) minmax(320px,.76fr);gap:16px}.panel{border:1px solid var(--line);border-radius:17px;background:linear-gradient(150deg,#172123dd,#121a1cdc);box-shadow:0 14px 40px #0003}.conversation{display:flex;min-height:650px;flex-direction:column;overflow:hidden}.conversation-head,.trace-head{display:flex;align-items:center;justify-content:space-between;padding:16px 18px;border-bottom:1px solid var(--line)}.conversation-head h2,.trace-head h2{margin:0;font-size:13px}.run-state{color:var(--muted);font:11px ui-monospace,SFMono-Regular,Menlo,monospace}.run-state.running{color:var(--amber)}.run-state.completed{color:var(--accent)}.run-state.failed{color:var(--rose)}.messages{display:grid;align-content:start;gap:17px;flex:1;padding:20px;overflow:auto}.message{max-width:91%;border:1px solid var(--line);border-radius:14px;padding:12px 14px;background:#111a1c}.message.harness{background:linear-gradient(135deg,#1d302a,#162223)}.message.user{justify-self:end;border-color:#73d9d044;background:#112a2b}.message-label{margin-bottom:6px;color:var(--cyan);font:10px ui-monospace,SFMono-Regular,Menlo,monospace;letter-spacing:.1em;text-transform:uppercase}.message p{margin:0;white-space:pre-wrap}.message .muted{color:var(--muted)}.composer{display:grid;gap:10px;padding:15px;border-top:1px solid var(--line);background:#101719}.composer textarea{min-height:104px;resize:vertical}.composer-meta{display:flex;align-items:center;justify-content:space-between;gap:10px}.hints{color:var(--faint);font-size:11px}.inline-fields{display:grid;grid-template-columns:1fr 1fr;gap:9px}.inline-fields .field{gap:5px}.danger-toggle{display:flex;align-items:center;gap:7px;color:var(--muted);font-size:11px}.danger-toggle input{accent-color:var(--accent)}.trace{min-height:650px;overflow:hidden}.trace-body{display:grid;gap:15px;padding:15px}.skill-focus{padding:12px;border:1px solid #a7ed762b;border-radius:11px;background:#17261d}.skill-focus .eyebrow{margin-bottom:5px}.skill-focus b{font-size:15px}.skill-focus p{margin:5px 0 0;color:var(--muted);font-size:12px}.summary-grid{display:grid;grid-template-columns:1fr 1fr;gap:8px}.metric{padding:10px;border:1px solid var(--line);border-radius:10px;background:#10191a}.metric span{display:block;color:var(--faint);font-size:10px;text-transform:uppercase}.metric b{display:block;margin-top:3px;font:600 16px ui-monospace,SFMono-Regular,Menlo,monospace}.section-title{display:flex;justify-content:space-between;margin:3px 2px 7px;color:var(--muted);font:10px ui-monospace,SFMono-Regular,Menlo,monospace;letter-spacing:.1em;text-transform:uppercase}.event-list{display:grid;gap:5px;max-height:330px;overflow:auto}.event{display:grid;grid-template-columns:28px 1fr;gap:8px;padding:8px;border-radius:8px;background:#10191a}.event-seq{color:var(--faint);font:11px ui-monospace,SFMono-Regular,Menlo,monospace}.event-type{overflow:hidden;color:#c2d0ce;font:11px ui-monospace,SFMono-Regular,Menlo,monospace;text-overflow:ellipsis;white-space:nowrap}.event-type.good{color:var(--accent)}.event-type.warn{color:var(--amber)}.event-type.bad{color:var(--rose)}.empty{padding:22px;border:1px dashed var(--line);border-radius:11px;color:var(--muted);text-align:center}.skill-grid,.agent-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(245px,1fr));gap:12px}.skill-card,.agent-card{position:relative;display:grid;gap:9px;min-height:170px;border:1px solid var(--line);border-radius:14px;padding:16px;background:linear-gradient(145deg,#172223,#11191a);text-align:left}.skill-card{cursor:pointer}.skill-card:hover,.skill-card.selected{border-color:#9de46d88;background:#1a2a22}.card-kicker{color:var(--cyan);font:10px ui-monospace,SFMono-Regular,Menlo,monospace;letter-spacing:.1em;text-transform:uppercase}.skill-card h2,.agent-card h2{margin:0;font-size:16px}.skill-card p,.agent-card p{margin:0;color:var(--muted);font-size:12px}.card-meta{align-self:end;color:var(--faint);font:10px ui-monospace,SFMono-Regular,Menlo,monospace;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.agent-card.selected{border-color:var(--cyan)}.detail-layout{display:grid;grid-template-columns:minmax(0,1.2fr) minmax(300px,.8fr);gap:16px}.detail-panel{padding:18px}.detail-panel h2{margin:0 0 12px;font-size:15px}.plan-step{margin:8px 0;border-left:2px solid var(--cyan);padding:9px 11px;background:#10191a}.plan-step b{display:block}.plan-step span{color:var(--muted);font-size:12px}.assessment{display:flex;align-items:center;justify-content:space-between;padding:11px;border-radius:9px;background:#10191a}.assessment.good{box-shadow:inset 2px 0 var(--accent)}.assessment.bad{box-shadow:inset 2px 0 var(--rose)}.run-lookup{display:flex;gap:8px;margin-bottom:13px}.run-lookup input{min-width:0;flex:1;border:1px solid var(--line);border-radius:9px;background:#0d1516;color:var(--ink);padding:9px}.code-block{max-height:340px;overflow:auto;border:1px solid var(--line);border-radius:11px;background:#0c1214;padding:13px;color:#b8cfca;font:11px/1.55 ui-monospace,SFMono-Regular,Menlo,monospace;white-space:pre-wrap}.run-output{max-height:190px}.tiny{color:var(--faint);font-size:11px}.divider{height:1px;margin:14px 0;background:var(--line)}#notice{position:fixed;right:20px;bottom:20px;z-index:8;display:none;max-width:min(440px,calc(100vw - 40px));border:1px solid var(--line);border-radius:11px;padding:11px 14px;background:#223031;box-shadow:var(--shadow);color:var(--ink)}@media (max-width:1040px){.work-grid,.detail-layout{grid-template-columns:1fr}.trace{min-height:0}.conversation{min-height:570px}}@media (max-width:760px){.app-shell{display:block}.sidebar{position:static;height:auto;border-right:0;border-bottom:1px solid var(--line)}.brand{padding-bottom:12px}.sidebar nav{display:flex;gap:5px;overflow:auto}.nav-label{display:none}.nav-button{width:auto;white-space:nowrap}.sidebar-bottom{display:none}.main{padding:17px 14px 30px}.topbar{margin-bottom:14px}.view-head{align-items:start;flex-direction:column}.inline-fields{grid-template-columns:1fr}.composer-meta{align-items:start;flex-direction:column}.skill-grid,.agent-grid{grid-template-columns:1fr}.auth-card{padding:25px}.status-pill{display:none}}.live-pane{border:1px solid var(--line);border-radius:12px;padding:11px 12px;margin-bottom:12px;background:var(--panel-2)}.stream-tools{display:flex;flex-wrap:wrap;gap:6px;margin:7px 0}.stream-tool{font:600 11px/1.3 ui-monospace,Menlo,monospace;color:var(--cyan);background:#0c1516;border:1px solid var(--line);border-radius:7px;padding:4px 7px}.stream-tool code{color:var(--muted);font-weight:400}.stream-text{white-space:pre-wrap;word-break:break-word;max-height:220px;overflow:auto;margin:0;color:var(--ink);font:12px/1.5 ui-monospace,Menlo,monospace}.message.assistant{border-left:3px solid var(--accent)}.assistant-text{white-space:pre-wrap;word-break:break-word;font-size:13px}.message-age{margin-top:6px;color:var(--faint);font-size:11px}.tool-activity{display:flex;flex-wrap:wrap;gap:6px;margin:8px 0}.tool-chip{font:600 11px/1.4 ui-monospace,Menlo,monospace;color:var(--cyan);background:#0c1516;border:1px solid var(--line);border-radius:999px;padding:2px 8px}.tool-chip.good{color:var(--accent);border-color:var(--accent)}.tool-chip.bad{color:var(--rose);border-color:var(--rose)}`;

export const CONSOLE_JS = `const $=s=>document.querySelector(s);const $$=s=>[...document.querySelectorAll(s)];
const state={token:localStorage.getItem('agentloop-token')||'',view:'workbench',user:null,skills:[],agents:[],tools:[],providers:[],defaultProviderKey:'',selectedSkillId:'',selectedAgentId:'auto',currentRun:null,running:false,noticeTimer:null,pollTimer:null,streamAbort:null};
async function api(path,options={}){const headers={accept:'application/json',...(options.body?{'content-type':'application/json'}:{}),...(state.token?{authorization:'Bearer '+state.token}:{})};let response;try{response=await fetch(path,{...options,headers:{...headers,...options.headers}})}catch(error){throw new Error('无法连接 AgentLoop 服务：'+(error instanceof Error?error.message:'网络错误'))}if(response.status===204)return undefined;const text=await response.text();let body={};try{body=text?JSON.parse(text):{}}catch{throw new Error('服务返回了无效 JSON')}if(!response.ok){const trace=body.error&&body.error.traceId?' (traceId: '+body.error.traceId+')':'';throw new Error((body.error&&body.error.message?body.error.message:response.statusText)+trace)}return body}
function escapeHtml(value){const node=document.createElement('div');node.textContent=String(value??'');return node.innerHTML}
function note(message){const target=$('#notice');target.textContent=message;target.style.display='block';clearTimeout(state.noticeTimer);state.noticeTimer=setTimeout(()=>target.style.display='none',4300)}
function selectedSkill(){return state.skills.find(item=>item.id===state.selectedSkillId)||state.skills[0]}
function selectedAgent(){return state.agents.find(item=>item.id===state.selectedAgentId)}
function eventTone(type){if(type==='run.completed'||type==='candidate.approved'||type==='skill.compliance.assessed')return'good';if(type.includes('failed')||type.includes('rejected'))return'bad';if(type.includes('started')||type.includes('pending')||type.includes('planned'))return'warn';return''}
function runClass(){if(!state.currentRun)return'';return state.currentRun.run.status}
function emptyDetail(){return{state:'pending',plan:{id:'',runId:'',version:0,status:'pending',goal:'计划生成中',steps:[]},assessments:[]}}
function phaseLabel(phase){return {planning:'规划中',execution:'执行中',assessment:'评估中',compaction:'压缩上下文'}[phase]||'处理中'}
function latestStreaming(){const run=state.currentRun;if(!run||!run.events)return null;for(let i=run.events.length-1;i>=0;i--){if(run.events[i].type==='assistant.streaming')return run.events[i]}return null}
function lastActivityAge(){const run=state.currentRun;if(!run||!run.events||!run.events.length)return null;const last=run.events[run.events.length-1];return Math.max(0,Math.round((Date.now()-last.createdAt)/1000))}
function streamToolCalls(toolCalls){if(!toolCalls||!toolCalls.length)return '';return '<div class="stream-tools">'+toolCalls.map(function(tc){return '<span class="stream-tool">⚙ '+escapeHtml(tc.name||'工具')+(tc.arguments?' <code>'+escapeHtml(String(tc.arguments).slice(0,120))+'</code>':'')+'</span>'}).join(' ')+'</div>'}
function livePane(){const run=state.currentRun;if(!run)return '';const stream=latestStreaming();if(!stream)return '';const d=stream.data||{};const age=lastActivityAge();return '<section class="live-pane"><div class="section-title"><span>实时输出 · '+escapeHtml(phaseLabel(d.phase))+'</span><span>'+(age===null?'':'活跃 '+age+'s 前')+'</span></div>'+streamToolCalls(d.toolCalls)+'<pre class="stream-text">'+escapeHtml(d.content||'（模型尚未产出文本）')+'</pre></section>'}
function eventLabel(event){const d=event.data||{};const t=event.type;if(t==='assistant.streaming')return '模型输出 · '+phaseLabel(d.phase);if(t==='assistant.committed')return '回合提交'+(d.finishReason?' · '+d.finishReason:'');if(t==='assistant.tool_call.committed')return '派发工具 '+(d.name||d.toolCallId||'');if(t==='tool.planned')return '准备工具 '+(d.toolName||'');if(t==='tool.completed')return '工具完成 '+(d.toolName||'');if(t==='tool.rejected')return '工具拒绝 '+(d.toolName||'');if(t==='tool.failed')return '工具失败 '+(d.toolName||'');if(t==='skill.activated')return '加载技能 '+(d.name||'');if(t==='skill.compliance.assessed')return '合规评估 '+(d.approved?'通过':'未通过');if(t==='plan.proposed')return '计划生成';if(t==='plan.admitted')return '计划接纳'+(d.steps?' · '+d.steps.length+' 步':'');if(t==='plan.step.started')return '步骤开始 '+(d.stepId||'');if(t==='plan.step.completed')return '步骤完成 '+(d.stepId||'');if(t==='candidate.approved')return '候选通过';if(t==='candidate.rejected')return '候选驳回';if(t==='run.completed')return 'Run 完成';if(t==='run.failed')return 'Run 失败';if(t==='run.cancelled')return 'Run 取消';return t}
function assistantBubble(){const stream=latestStreaming();if(!stream)return '';const d=stream.data||{};const age=lastActivityAge();return '<article class="message assistant"><div class="message-label">Harness · '+escapeHtml(phaseLabel(d.phase))+'</div>'+streamToolCalls(d.toolCalls)+'<div class="assistant-text">'+escapeHtml(d.content||'…')+'</div>'+(age===null?'':'<div class="message-age">'+age+'s 前</div>')+'</article>'}
function toolActivity(){const run=state.currentRun;if(!run||!run.events)return '';const tools=run.events.filter(function(e){return e.type==='tool.planned'||e.type==='tool.completed'||e.type==='assistant.tool_call.committed'||e.type==='skill.activated'}).slice(-3);if(!tools.length)return '';return '<div class="tool-activity">'+tools.map(function(e){return '<span class="tool-chip '+eventTone(e.type)+'">'+eventLabel(e)+'</span>'}).join('')+'</div>'}
function stopPolling(){if(state.pollTimer!==null){clearTimeout(state.pollTimer);state.pollTimer=null}if(state.streamAbort!==null){state.streamAbort.abort();state.streamAbort=null}}
function renderAuth(){const root=$('#auth-root');const app=$('#app-root');app.hidden=true;root.hidden=false;root.innerHTML='<main class="auth-shell"><section class="auth-card"><p class="eyebrow">AgentLoop / DeepSeek Harness</p><h1>让每一次运行都可追踪</h1><p>登录后直接选择已发现的 Skill，在同一个界面查看 Plan、工具证据、合规评估和终态。</p><form id="auth-form" class="stack"><label class="field">邮箱<input name="email" type="email" autocomplete="username" placeholder="you@example.com" required></label><label class="field">密码<input name="password" type="password" autocomplete="current-password" minlength="12" required></label><div class="button-row"><button class="button" name="mode" value="login">登录</button><button class="button secondary" name="mode" value="register">注册新账号</button></div></form></section></main>'}
function authMode(submitter){return submitter instanceof HTMLButtonElement&&(submitter.value==='login'||submitter.value==='register')?submitter.value:'login'}
function navButton(view,icon,label){return'<button class="nav-button '+(state.view===view?'active':'')+'" data-view="'+view+'"><span class="nav-icon">'+icon+'</span>'+label+'</button>'}
function renderShell(){const root=$('#app-root');const auth=$('#auth-root');auth.hidden=true;root.hidden=false;root.innerHTML='<div class="app-shell"><aside class="sidebar"><div class="brand"><span class="brand-mark">H</span><div><strong>DeepSeek Harness</strong><small>powered by AgentLoop</small></div></div><nav><p class="nav-label">Runtime</p>'+navButton('workbench','01','Harness 工作台')+navButton('runs','02','Run 证据')+'<p class="nav-label">Catalog</p>'+navButton('skills','03','已有 Skills')+navButton('agents','04','Agents')+'</nav><div class="sidebar-bottom"><div class="identity"><b>'+escapeHtml(state.user.email)+'</b>本地 Token，仅存浏览器会话</div><button class="nav-button" data-action="logout"><span class="nav-icon">×</span>退出登录</button></div></aside><main class="main"><header class="topbar"><div class="crumb">Harness / <b>'+({workbench:'运行工作台',runs:'Run 证据',skills:'已有 Skills',agents:'Agents'}[state.view]||'工作台')+'</b></div><div class="status-pill"><i class="status-dot"></i> '+escapeHtml(providerLabel())+'</div></header><div id="view-root"></div></main></div>';renderView()}
function providerLabel(){const provider=state.providers.find(item=>item.key===state.defaultProviderKey)||state.providers[0];return provider?provider.key+' / '+provider.defaultModel:'未配置 Provider'}
function renderView(){const target=$('#view-root');if(!target)return;if(state.view==='skills')target.innerHTML=renderSkills();else if(state.view==='agents')target.innerHTML=renderAgents();else if(state.view==='runs')target.innerHTML=renderRuns();else target.innerHTML=renderWorkbench()}
function renderWorkbench(){const skill=selectedSkill();const run=state.currentRun;const events=run?run.events:[];const detail=run?run.detail:null;const output=run&&run.run.output?run.run.output:'尚未运行。选择一个已有 Skill，输入任务后开始。';return'<section class="view-head"><div><p class="eyebrow">Plan-first / auditable execution</p><h1>Harness 工作台</h1><p>模拟 DeepSeek Harness 的运行视图：模型负责计划与工具调用；Runtime 负责 Skill 激活、证据、Assessment 和最终提交。</p></div><button class="button ghost" data-view="runs">查看 Run 证据</button></section><div class="work-grid"><section class="panel conversation"><header class="conversation-head"><h2>当前任务</h2><span class="run-state '+runClass()+'">'+(state.running?(latestStreaming()?phaseLabel(latestStreaming().data.phase):'模型处理中…'):run?escapeHtml(run.run.status):'准备就绪')+'</span></header><div class="messages"><article class="message harness"><div class="message-label">Harness</div><p>选择服务端已发现的 Skill。界面只会按需创建并复用测试 Agent，<b>不会创建新的 Skill</b>。</p></article>'+(run?'<article class="message user"><div class="message-label">Task</div><p>'+escapeHtml(run.run.input)+'</p></article>'+assistantBubble()+toolActivity()+'<article class="message harness"><div class="message-label">Outcome</div><p>'+escapeHtml(output)+'</p></article>':'<article class="message"><div class="message-label">Next</div><p class="muted">运行后，这里会展示 Runtime 最终接受的输出；Plan 与事件在右侧持续可查。</p></article>')+'</div><form id="task-form" class="composer"><div class="inline-fields"><label class="field">Skill<select id="skill-select">'+state.skills.map(item=>'<option value="'+escapeHtml(item.id)+'" '+(skill&&item.id===skill.id?'selected':'')+'>'+escapeHtml(item.name)+'</option>').join('')+'</select></label><label class="field">运行身份<select id="agent-select"><option value="auto" '+(state.selectedAgentId==='auto'?'selected':'')+'>Harness 测试 Agent（自动）</option>'+state.agents.map(item=>'<option value="'+escapeHtml(item.id)+'" '+(state.selectedAgentId===item.id?'selected':'')+'>'+escapeHtml(item.name)+' · '+escapeHtml(item.modelId)+'</option>').join('')+'</select></label></div><textarea name="input" placeholder="例如：请为海岸渡轮档案设计一个可访问的单页 HTML；写入后在同一步读回验证。" required></textarea><div class="composer-meta"><label class="danger-toggle"><input name="allowDangerousTools" type="checkbox"> 允许本次 Run 写文件、执行命令或 GUI 副作用</label><span class="hints">Run 会等待 Plan、工具证据与 Assessment</span><button class="button" '+(state.running||!skill?'disabled':'')+'>'+ (state.running?'Harness 正在运行…':'启动 Harness Run')+'</button></div></form></section><aside class="panel trace"><header class="trace-head"><h2>可审计轨迹</h2><span class="tiny">'+(run?'#'+escapeHtml(run.run.id.slice(0,8)):'等待运行')+'</span></header><div class="trace-body">'+(skill?'<section class="skill-focus"><p class="eyebrow">Bound package</p><b>'+escapeHtml(skill.name)+'</b><p>'+escapeHtml(skill.description)+'</p></section>':'<div class="empty">服务端没有可用 Skill Package</div>')+'<div class="summary-grid"><div class="metric"><span>Plan steps</span><b>'+ (detail?detail.plan.steps.length:'—')+'</b></div><div class="metric"><span>Assessments</span><b>'+ (detail?detail.assessments.length:'—')+'</b></div></div>'+(state.currentRun?livePane():'')+'<section><div class="section-title"><span>Event timeline</span><span>'+events.length+'</span></div><div class="event-list">'+(events.length?events.slice(-18).map(event=>'<div class="event"><span class="event-seq">'+event.seq+'</span><span class="event-type '+eventTone(event.type)+'">'+eventLabel(event)+'</span></div>').join(''):'<div class="empty">事件会在 Run 结束后从权威 API 读取。</div>')+'</div></section></div></aside></div>'}
function renderSkills(){return'<section class="view-head"><div><p class="eyebrow">Server discovered / private materialization</p><h1>已有 Skill Packages</h1><p>选择任一 Package 后返回工作台即可直接验证。默认流不要求创建新 Skill。</p></div><button class="button" data-view="workbench">进入工作台</button></section><section class="skill-grid">'+(state.skills.length?state.skills.map(skill=>'<button class="skill-card '+(skill.id===state.selectedSkillId?'selected':'')+'" data-action="select-skill" data-id="'+escapeHtml(skill.id)+'"><span class="card-kicker">'+escapeHtml(skill.sourceKind)+' package</span><h2>'+escapeHtml(skill.name)+'</h2><p>'+escapeHtml(skill.description)+'</p><span class="card-meta">'+escapeHtml(skill.id)+'</span></button>').join(''):'<div class="empty">服务端当前未发现 Package。请检查 SKILL_DIRECTORY。</div>')+'</section>'}
function renderAgents(){return'<section class="view-head"><div><p class="eyebrow">Saved profiles</p><h1>Agents</h1><p>可以选择已有 Agent，也可在工作台选择“自动”来为当前 Skill 创建并复用一个 Harness 测试 Agent。</p></div><button class="button" data-view="workbench">返回工作台</button></section><section class="agent-grid">'+(state.agents.length?state.agents.map(agent=>'<button class="agent-card '+(agent.id===state.selectedAgentId?'selected':'')+'" data-action="select-agent" data-id="'+escapeHtml(agent.id)+'"><span class="card-kicker">'+escapeHtml(agent.providerKey)+' / '+escapeHtml(agent.modelId)+'</span><h2>'+escapeHtml(agent.name)+'</h2><p>Skills '+agent.skillIds.length+' · Tools '+agent.toolNames.length+'</p><span class="card-meta">'+escapeHtml(agent.id)+'</span></button>').join(''):'<div class="empty">还没有 Agent。使用工作台的“自动”身份即可创建测试 Agent。</div>')+'</section>'}
function renderRuns(){const run=state.currentRun;const detail=run?run.detail:null;return'<section class="view-head"><div><p class="eyebrow">Canonical run state</p><h1>Run 证据</h1><p>这里展示服务端权威 Run、Plan、Skill Compliance 与事件；模型文本或单个 Tool 成功本身不等于完成。</p></div><button class="button" data-view="workbench">新建任务</button></section><div class="detail-layout"><section class="panel detail-panel"><form id="run-lookup" class="run-lookup"><input name="runId" placeholder="输入已有 Run ID" value="'+(run?escapeHtml(run.run.id):'')+'"><button class="button secondary">载入</button></form>'+(run?'<h2>Run '+escapeHtml(run.run.id)+'</h2><p class="tiny">状态：<b>'+escapeHtml(run.run.status)+'</b> · Agent '+escapeHtml(run.run.agentId)+'</p><div class="divider"></div><h2>Plan</h2>'+detail.plan.steps.map(step=>'<div class="plan-step"><b>'+escapeHtml(step.id)+' · '+escapeHtml(step.status)+'</b><span>'+escapeHtml(step.objective)+'</span></div>').join('')+'<div class="divider"></div><h2>Output</h2><pre class="code-block run-output">'+escapeHtml(run.run.output||'无终态输出')+'</pre>':'<div class="empty">运行一次任务，或输入 Run ID 载入权威记录。</div>')+'</section><aside class="panel detail-panel"><h2>Assessment</h2>'+(detail&&detail.assessments.length?detail.assessments.map(assessment=>'<div class="assessment '+(assessment.approved?'good':'bad')+'"><span>Step '+escapeHtml(assessment.stepId)+' · attempt '+assessment.attempt+'</span><b>'+ (assessment.approved?'通过':'未通过')+'</b></div>').join(''):'<div class="empty">尚无 Assessment</div>')+'<div class="divider"></div><h2>Events</h2><div class="event-list">'+(run?run.events.map(event=>'<div class="event"><span class="event-seq">'+event.seq+'</span><span class="event-type '+eventTone(event.type)+'">'+eventLabel(event)+'</span></div>').join(''):'<div class="empty">尚无事件</div>')+'</div></aside></div>'}
async function refresh(){if(!state.token){renderAuth();return}try{const me=await api('/v1/me');const results=await Promise.all([api('/v1/skills'),api('/v1/agents'),api('/v1/tools'),api('/v1/providers')]);state.user=me.user;state.skills=results[0].skills;state.agents=results[1].agents;state.tools=results[2].tools;state.providers=results[3].providers;state.defaultProviderKey=results[3].defaultProviderKey||'';if(!state.skills.some(item=>item.id===state.selectedSkillId))state.selectedSkillId=state.skills[0]?state.skills[0].id:'';if(state.selectedAgentId!=='auto'&&!state.agents.some(item=>item.id===state.selectedAgentId))state.selectedAgentId='auto';renderShell()}catch(error){state.token='';localStorage.removeItem('agentloop-token');note(error instanceof Error?error.message:'登录已失效');renderAuth()}}
async function ensureHarnessAgent(skill){const name='Harness · '+skill.name;let agent=state.agents.find(item=>item.name===name&&item.skillIds.includes(skill.id));if(agent)return agent;const provider=state.providers.find(item=>item.key===state.defaultProviderKey)||state.providers[0];if(!provider)throw new Error('服务端没有已配置的 Provider');const body=await api('/v1/agents',{method:'POST',body:JSON.stringify({name,systemPrompt:'You are a test runner for one bound Skill in a plan-first runtime. Follow the exact loaded Skill instructions. Do not claim completion without persisted evidence and Assessment.',providerKey:provider.key,modelId:'default',skillIds:[skill.id],childAgentIds:[],toolNames:state.tools.map(tool=>tool.name)})});state.agents.push(body.agent);note('已创建并绑定 '+skill.name+' 的 Harness 测试 Agent；未创建新 Skill。');return body.agent}
async function loadRun(runId){const runBody=await api('/v1/runs/'+encodeURIComponent(runId));const eventsBody=await api('/v1/runs/'+encodeURIComponent(runId)+'/events');const detail=await api('/v1/runs/'+encodeURIComponent(runId)+'/plan');state.currentRun={run:runBody.run,detail,events:eventsBody.events};state.running=runBody.run.status==='running';renderShell()}
function pollRun(runId){stopPolling();const tick=async()=>{try{await loadRun(runId);if(state.currentRun&&state.currentRun.run.status==='running'){state.pollTimer=setTimeout(tick,1200)}else{stopPolling();note('Run 已结束并载入权威 Plan、事件与 Assessment。')}}catch(error){stopPolling();state.running=false;renderShell();note(error instanceof Error?error.message:'轮询 Run 状态失败')}};void tick()}
function subscribeRun(runId){stopPolling();const controller=new AbortController();state.streamAbort=controller;let renderTimer=null;const scheduleRender=function(){if(renderTimer!==null)return;renderTimer=setTimeout(function(){renderTimer=null;renderShell()},80)};(async function(){try{const response=await fetch('/v1/runs/'+encodeURIComponent(runId)+'/events/stream',{headers:{authorization:'Bearer '+state.token},signal:controller.signal});if(!response.ok||!response.body){note('无法订阅 Run 流：HTTP '+response.status);state.running=false;return}const reader=response.body.getReader();const decoder=new TextDecoder();let buffer='';const NL=String.fromCharCode(10);for(;;){const step=await reader.read();if(step.done)break;buffer+=decoder.decode(step.value,{stream:true});const chunks=buffer.split(NL+NL);buffer=chunks.pop()||'';for(const chunk of chunks){const line=chunk.split(NL).find(function(l){return l.indexOf('data: ')===0});if(!line)continue;let event;try{event=JSON.parse(line.slice(6))}catch{continue}if(!state.currentRun)continue;const list=state.currentRun.events||[];if(!list.some(function(e){return e.seq===event.seq})){list.push(event);list.sort(function(a,b){return a.seq-b.seq})}state.currentRun.events=list;if(event.type==='run.completed'||event.type==='run.failed'||event.type==='run.cancelled'){controller.abort();state.streamAbort=null;state.running=false;void loadRun(runId).catch(function(){});note('Run 已结束并载入权威 Plan、事件与 Assessment。');return}scheduleRender()}}}catch(error){if(controller.signal.aborted)return;state.running=false;scheduleRender();note(error instanceof Error?error.message:'Run 流订阅中断')}})()}
async function startRun(form){const input=String(new FormData(form).get('input')||'').trim();const skill=selectedSkill();if(!input)throw new Error('请输入任务内容');if(!skill)throw new Error('请先选择已有 Skill');const requestedAgent=state.selectedAgentId==='auto'?await ensureHarnessAgent(skill):selectedAgent();if(!requestedAgent)throw new Error('未找到所选 Agent');stopPolling();state.running=true;renderShell();const body=await api('/v1/runs/async',{method:'POST',body:JSON.stringify({agentId:requestedAgent.id,input,allowDangerousTools:new FormData(form).get('allowDangerousTools')==='on'})});state.currentRun={run:body.run,detail:emptyDetail(),events:[]};state.running=true;renderShell();subscribeRun(body.run.id)}
document.addEventListener('submit',async event=>{const form=event.target;if(!(form instanceof HTMLFormElement))return;event.preventDefault();try{if(form.id==='auth-form'){const data=new FormData(form);const mode=authMode(event.submitter);const body=await api('/v1/auth/'+mode,{method:'POST',body:JSON.stringify({email:data.get('email'),password:data.get('password')})});state.token=body.token;localStorage.setItem('agentloop-token',state.token);await refresh()}else if(form.id==='task-form'){await startRun(form)}else if(form.id==='run-lookup'){const id=String(new FormData(form).get('runId')||'').trim();if(!id)throw new Error('请输入 Run ID');await loadRun(id)}}catch(error){state.running=false;if(state.token&&state.user)renderShell();else renderAuth();note(error instanceof Error?error.message:'操作失败')}});
document.addEventListener('change',event=>{const input=event.target;if(!(input instanceof HTMLSelectElement))return;if(input.id==='skill-select'){state.selectedSkillId=input.value;renderShell()}if(input.id==='agent-select'){state.selectedAgentId=input.value;renderShell()}});
document.addEventListener('click',async event=>{const element=event.target instanceof Element?event.target.closest('[data-action],[data-view]'):null;if(!element)return;try{if(element.dataset.view){state.view=element.dataset.view;renderShell();return}if(element.dataset.action==='logout'){stopPolling();try{await api('/v1/auth/logout',{method:'POST'})}catch{}state.token='';state.currentRun=null;localStorage.removeItem('agentloop-token');renderAuth();return}if(element.dataset.action==='select-skill'){state.selectedSkillId=element.dataset.id||'';state.view='workbench';renderShell();return}if(element.dataset.action==='select-agent'){state.selectedAgentId=element.dataset.id||'auto';state.view='workbench';renderShell()}}catch(error){note(error instanceof Error?error.message:'操作失败')}});
refresh();`;
